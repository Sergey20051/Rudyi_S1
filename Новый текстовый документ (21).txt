def count_equal_divisors(n):
    count = 0
    for m in range(1, n):
        if n % (m + 1) == 0:
            count += 1
    return count


n = 20

print(f"Кількість рівних дільників числа {n}: {count_equal_divisors(n)}")
